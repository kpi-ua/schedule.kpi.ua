import React from 'react';
import style from './style.scss';

export const NameSearch = ({}) => (
  <React.Fragment>
    <div>!TODO: create name search for teachers page</div>
  </React.Fragment>
);

export default NameSearch;
