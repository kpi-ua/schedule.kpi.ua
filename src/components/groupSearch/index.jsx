import React from 'react';
import style from './style.scss';

export const GroupSearch = ({}) => (
  <React.Fragment>
    <div>!TODO: create group search</div>
  </React.Fragment>
);

export default GroupSearch;
